import 'package:flutter/material.dart';

const Color mainColor = Colors.white;
const Color midColor = Color.fromARGB(90, 243, 223, 223);
const Color minorColor = Color.fromARGB(255, 23, 23, 23);
const Color callToAction = Colors.red;
const Color white = Colors.white;

Size size(context) {
  final Size size = MediaQuery.of(context).size;
  return size;
}
